name: ci

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  web:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      VITE_SUPABASE_PROJECT_ID: ci-project-id
      VITE_SUPABASE_PUBLISHABLE_KEY: ci-publishable-key
      VITE_SUPABASE_URL: https://ci-project-id.supabase.co
      GROQ_API_KEY: ci-groq-key
      TAVILY_API_KEY: ci-tavily-key
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/web/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: apps/web

      - name: Install Playwright Chromium
        run: TMPDIR=/tmp TEMP=/tmp TMP=/tmp PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install --with-deps chromium
        working-directory: apps/web

      - name: Validate environment
        run: npm run validate-env
        working-directory: apps/web

      - name: Build
        run: npm run build
        working-directory: apps/web

      - name: Lint
        run: npm run lint
        working-directory: apps/web

      - name: Test
        run: npm run test
        working-directory: apps/web

      - name: Playwright E2E
        run: npm run test:e2e
        working-directory: apps/web
